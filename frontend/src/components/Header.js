import React from 'react';
import './Header.css';

function Header() {
    return (
      <header className="header-info">
        {/* Header content such as Devices Online/Offline, IP Addresses, etc. */}
      </header>
    );
  }

  export default Header;